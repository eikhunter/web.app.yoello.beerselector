import React from 'react';

import Beer from '../../../constants/Beer';

interface Props {
    item: Beer;
}

const ProductBasketItem: React.FC<Props> = ({ item }) => {
    return (
        <div>

        </div>
    );
};

export default ProductBasketItem;
